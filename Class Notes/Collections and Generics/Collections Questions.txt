Collections Questions:-
=-=-=-=-=-=-=-=-=-=-=-=
1. K Closest Numbers
---------------------
- You are given an array of integers and a target value. Design a system to return K closest numbers to the target.
- Return k closest numbers
- If tie → prefer smaller number
- Output sorted in ascending order

Input:-
nums = [1, 4, 4, 6], K = 3, Target = 3
Output:-
[1, 4, 4]


Solution:-
--------
public class Main {
	public static void main(String[] args) {
		int[] arr = { 10, 4, 6, 7, 2, 21, 10, 1, 15 };
		int tar = 9;
		int k = 4;

		List<Integer> list = new ArrayList<Integer>();
		for (int i = 0; i < arr.length; i++) {
			list.add(arr[i]);
		}

		Collections.sort(list, (a, b) -> {
			int d1 = Math.abs(tar - a);
			int d2 = Math.abs(tar - b);
			if (d1 != d2) {
				return d1 - d2;
			}
			return a - b;
		});
		for (int i = 0; i < k; i++) {
			System.out.print(list.get(i) + " ");
		}
	}
}
Output:-
10 10 7 6 


2. Frequency Sort with Order Preservation
------------------------------------------
- Given an array, sort elements based on:

	1. Increasing frequency
	2. If frequency same → smaller number first

Example

Input: [4, 5, 6, 5, 4, 3]
Output: [6, 3, 4, 4, 5, 5]


3. Custom Sorted Student Ranking
--------------------------------
You are given a list of students. Sort them using TreeMap based on:

1. Higher marks first
2. If marks same → lexicographically smaller name
3. If still same → smaller ID

Class Design
-------------
class Student {
    int id;
    String name;
    int marks;
}

- Store students in TreeMap
- Implement Comparator


4. Product Catalog Sorting
-------------------------
- Design a system to store products sorted by:
	1. Price (ascending)
	2. If same → rating (descending)
	3. If same → name

Class Design:-
-----------
class Product implements Comparable<Product> {
    int id;
    String name;
    double price;
    double rating;
}

- Override `compareTo()`
- Store products in TreeMap



5. Employee Record System
---------------------------

Store employee records in HashMap such that:
- Two employees are same if:
	- same employeeId
	- same email

---

Class Design:-
-------------

class Employee {
    int id;
    String name;
    String email;
}

Override:
	- equals()
	- hashCode()
	- Use Employee as key in HashMap


6. Custom Cache Key System
--------------------------

Design a cache where key is a combination of:

- userId
- resourceId

Two keys are equal if both fields match.

---

Class Design:-
-------------

class CacheKey {
    int userId;
    int resourceId;
}

- Override equals() and hashCode()
- Use in HashMap / LinkedHashMap


7. Closest Ceiling & Floor Finder
----------------------------------
- You are given an array of integers and multiple query values.
- For each query, find:
	- the floor (greatest element ≤ target)
	- the ceiling (smallest element ≥ target)

For each query print:
- floor and ceiling
-If not present, print -1

Example:-
---------
Input:
nums = [2, 5, 8]
queries = [4, 8, 1]

Output:
2 5
8 8
-1 2

8. Check Pair with Given Sum
------------------------------
Given an array and a target sum, determine whether there exists a pair of elements whose sum equals the target. Array may be sorted or not. 

Example
Input: nums = [2, 7, 11, 15], target = 9
Output: true

- Find the solution in O(n).

9. First Unique Element in Array
--------------------------------
- Find the first element in the array that appears only once.

Example
Input: [4, 5, 1, 2, 0, 4]
Output: 5

Exception Handling:-
-==-=-=-=-=-=-=--=-=

10. Online Voting System Using Custom Exception
-----------------------------------------------
- Write a Java program to simulate an online voting system.
- Create a class Voter with the following data members:	
	- Voter ID
	- Voter Name
	- Age
- The user is allowed to vote only if age is 18 or above.
- Create a user-defined exception named:
	- InvalidAgeException

- Throw this exception if the voter age is less than 18.

- If eligible, display:

	Voting Allowed
	
Sample Input
101
Rahul
16

Sample Output
Exception: Age must be 18 or above

11. File Data Reader Using Checked Exception
------------------------------------------
- Write a Java program to read integer values from a text file and calculate their sum.
- The file name should be provided by the user.
- Handle the following checked exceptions properly:
	FileNotFoundException
	IOException
Use:
- throws
- try-catch-finally

- If the file is not found, display an appropriate message.

Sample Input
numbers.txt

Assume file contents:
10 20 30 40

Sample Output
Sum = 100

12. ATM PIN Verification Using Unchecked Exception
------------------------------------------------
- Write a Java program to simulate ATM PIN verification.
- Create a class ATM containing the following data members:
	Account Number
	PIN
- Create a method:
	void verifyPin(int enteredPin)
	Inside the method:
		- If the entered PIN is incorrect, throw an IllegalArgumentException with the message:
			- Invalid PIN
		- Otherwise display:
			- Access Granted
Functional Requirements:-
- The user gets a maximum of 3 attempts to enter the correct PIN.
- If the correct PIN is entered within 3 attempts:
	- Display "Access Granted"
	- Stop further execution.
- If all 3 attempts fail, display:
	- Card Blocked
- Handle the exception using try-catch.

Sample Input
1234
1111
2222
1234

Explanation
First input → Original ATM PIN
Next inputs → User entered PINs

Sample Output
Invalid PIN
Invalid PIN
Access Granted

Another Sample Input
1234
1111
2222
3333

Sample Output
Invalid PIN
Invalid PIN
Invalid PIN
Card Blocked

Constraints-
PIN should contain exactly 4 digits.
Maximum attempts allowed = 3.

13. Employee Salary Validation Using throw and throws
------------------------------------------------
- Write a Java program to manage employee salary details.
- Create a class Employee with:
	Employee ID
	Employee Name
	Salary
- Create a method:
	validateSalary(double salary) throws Exception
		- If salary is less than 50000, throw an exception with the message:
			- Salary cannot be less than 50000
		- Otherwise display:
			- Valid Salary

Sample Input
101
Aman
35500
Sample Output
Exception: Salary cannot be less than 50000


One question for revising 3-4 topics:-
-------------------------------------
=====================================
14. Smart Library Management System Using Core Java
-----------------------------------------------
- Write a Java program to develop a Smart Library Management System for maintaining library book issue records using Core Java concepts.

- Create a class Book with the following data members:
	Book ID
	Book Name
	Author Name
	Student Name
	Issue Date (dd-MM-yyyy)
	Return Status (Issued / Returned)
	
Functional Requirements
=-=-=-=-=-=-=-=-=-=-=
1. Store Book Records
	Store all book objects using:
		ArrayList<Book>

2. Remove Duplicate Records
- Two book records are considered duplicates if:
	Book ID is the same
	Student Name is same
- Students must Override:
	equals()
	hashCode()
- Use a suitable Collection (HashSet) to remove duplicate records.
- Display:
	Duplicate Records Removed Successfully

3. Sort Book Records
(a)
- Students must implement:
	Comparable<Book>
- Sort books according to:
	Student Name alphabetically
	If student names are same, sort by Fine Amount in descending order
- Display all sorted records.

(b)
- Students must implement:
	Comparator<Book>
- Sort books according to:
	Issue Date
	If the dates are the same, sort by Book ID in ascending order
- Display all sorted records.

4. String Processing
- Using String class methods, display:
(a) Total number of books issued to students whose names start with a vowel:
	A, E, I, O, U
(b) The longest book title present in the library records.
(c)Total number of books whose title contains the word:
	Java
- Students should use String methods such as:
	contains()
	startsWith()
	length()
	equalsIgnoreCase()

5. Exception Handling
- Create a custom exception:
	InvalidBookIdException
- Throw the exception if:
	Book ID <= 0
- Display:
	Exception: Invalid Book ID
- Students must:
	- Create a user-defined exception class.
- Use:
	throw
	try-catch


6. Collection-Based Searching
- Search and display all books issued by a particular author name entered by the user.
- If no record is found, display:
	No Records Found

Sample Input
5
101 JavaProgramming James Rahul 12-04-2026 Issued 
102 DBMS Korth Aman 15-04-2026 Returned 
103 DataStructures Mark Ankit 18-04-2026 Issued 
101 JavaProgramming James Rahul 12-04-2026 Issued 
104 OperatingSystems Galvin Esha 20-04-2026 Returned

Sample Output
Duplicate Records Removed Successfully

Sorted Book Records:
102 DBMS Korth Aman 15-04-2026 Returned 
103 DataStructures Mark Ankit 18-04-2026 Issued 
104 OperatingSystems Galvin Esha 20-04-2026 Returned 
101 JavaProgramming James Rahul 12-04-2026 Issued 

Books issued to students with vowel names: 2

Longest Book Title:
OperatingSystems

Books containing 'Java' in title:
1

Books issued by author James:
101 JavaProgramming James Rahul 12-04-2026 Issued

- Requirements
	Students must use:
		- Collection Framework
		- ArrayList
		- HashSet
		- OOP Concepts
		- Class and Objects
		- Constructor
		- Method Overriding
		- Override Methods
			- equals()
			- hashCode()
		- Sorting
			- Comparable<Book>
		- Exception Handling
			- Custom Exception
			- throw
			- try-catch
		- String Processing		
			- Use suitable String methods.

Constraints:-
- Book ID must be positive.
- Return Status must be either:
	Issued
	Returned
- Number of records ≥ 1.

Note:-
Students should:
- Use meaningful variable names.
- Avoid duplicate records properly using the Collection Framework.
- Display output in proper formatted manner.
